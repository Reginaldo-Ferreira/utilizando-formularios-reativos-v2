import { MaskDiretivaDirective } from './mask-diretiva.directive';

describe('MaskDiretivaDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskDiretivaDirective();
    expect(directive).toBeTruthy();
  });
});
