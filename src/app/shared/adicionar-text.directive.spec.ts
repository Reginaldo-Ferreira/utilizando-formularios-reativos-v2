import { AdicionarTextDirective } from './adicionar-text.directive';

describe('AdicionarTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AdicionarTextDirective();
    expect(directive).toBeTruthy();
  });
});
