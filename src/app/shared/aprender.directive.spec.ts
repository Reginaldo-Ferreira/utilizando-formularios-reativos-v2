import { AprenderDirective } from './aprender.directive';

describe('AprenderDirective', () => {
  it('should create an instance', () => {
    const directive = new AprenderDirective();
    expect(directive).toBeTruthy();
  });
});
