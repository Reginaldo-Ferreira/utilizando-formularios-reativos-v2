import { AddElemErroDirective } from './add-elem-erro.directive';

describe('AddElemErroDirective', () => {
  it('should create an instance', () => {
    const directive = new AddElemErroDirective();
    expect(directive).toBeTruthy();
  });
});
