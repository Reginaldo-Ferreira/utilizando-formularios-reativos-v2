import { RetirTagPaiDirective } from './retir-tag-pai.directive';

describe('RetirTagPaiDirective', () => {
  it('should create an instance', () => {
    const directive = new RetirTagPaiDirective();
    expect(directive).toBeTruthy();
  });
});
